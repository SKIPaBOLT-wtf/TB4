# IP-17 Evidence - Core Python Models

Date: 2026-09-26

## Verified

- Added immutable typed models for operation IDs, device IDs, artifact IDs, generation counters, epoch seconds, reason codes, and object/state references.
- Added canonical typed roles, logical objects, FETCH_BALL result codes, and state filename construction.
- Models remain transport-agnostic and contain no deployment-specific defaults.
- Artifact ID validation follows the canonical schema instead of inventing an unsupported character restriction.
- Tests prove Python role/object/result enums exactly match canonical YAML registries.
- Tests prove every canonical stateful filename generated by Python matches the YAML registry/state-machine combination.
- Identifier round-trip, invalid generation, malformed identifiers, state-reference round-trip, and fixed-object misuse are covered.
- GitHub Actions CI run `36198117583` completed successfully for commit `e0ce8afeb909c0ab09ada1a3af341d485da13867`.

## Result

IP-17 completion criteria are satisfied.
